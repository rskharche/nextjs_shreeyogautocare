export default function AboutLayout({ children }: LayoutProps<"/">) {
  return (
    <html>
       
      <body className="min-h-full flex flex-col">
        
        {children}</body>
    </html>
  );
}
